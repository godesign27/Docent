# contracts/

Generated by `docent ingest`, one folder per client:

```
contracts/<client-id>/contract.json   normalized contract
contracts/<client-id>/gaps.md         human-readable gap report
```

Everything here except this README is git-ignored. Client contracts never get committed to the core Docent repo, so no clone carries another client's design-system data.
